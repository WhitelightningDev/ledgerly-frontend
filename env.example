# API base URL used by the browser (public env var).
#
# Local dev:
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
#
# Vercel (Production):
# Set `NEXT_PUBLIC_API_URL` to `https://ledgerly-backend-lau2.onrender.com`
